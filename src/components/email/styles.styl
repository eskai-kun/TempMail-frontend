.Email
    & .address
        display: flex
        justify-content: center
        & > *
            margin-right: 5px
        & .copy
            background: var(--primary-color);
            color: var(--primary-color-text);
            display: flex
            align-content: center
            justify-content: center
            padding: 8px
            cursor pointer
            border-radius: 10px
            transition: .5s
            &:hover
                background var(--background-boxs)
                color: var(--background-boxs-text)
        & input
            background var(--background-boxs)
            color: var(--background-boxs-text)
            outline: 0
            border: 0
            width: 350px
            max-width: 80%
            padding: 5px
            font-size: 19px
            border-radius: 5px
            text-align: center
            font-family: 'Acme'
            
            